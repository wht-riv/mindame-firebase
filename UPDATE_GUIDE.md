# v5.2 更新手順

## Firebase
v5.1用のルールを公開済みなら、Firebase側の変更は不要です。`goalText` は既存の公開投稿へ追加するフィールドとして保存されます。

## GitHub
1. ZIPを展開します。
2. `mindame-firebase-v52` フォルダーの中身をGitHubリポジトリ直下へアップロードします。
3. `package.json` と `package-lock.json` を必ずセットで更新します。
4. `Update Mindame to v5.2` としてmainへコミットします。
5. Actions成功後、公開URLを `?v=5.2` 付きで確認します。

## テスト項目
- 初期表示が「すべて」になっている
- 各ジャンルへ絞り込める
- 投稿カード上部にジャンルと目標が表示される
- 共有前の確認画面にも目標が表示される
- 🌱が即時反映される
